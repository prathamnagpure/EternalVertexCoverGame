import React from 'react';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

export default function ExportIcon(props) {
  return <Icon name="file-export-outline" size={30} color="gray" {...props} />;
}
